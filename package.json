{
  "name": "gaunle-swad-build",
  "version": "1.0.0",
  "description": "Build all 4 Gaunle Swad frontend apps for production deployment",
  "scripts": {
    "build:customer": "cd Frontend/customer && npm run build",
    "build:admin": "cd Frontend/admin && npm run build",
    "build:kitchen": "cd Frontend/kitchen && npm run build",
    "build:rider": "cd Frontend/rider && npm run build",
    "build:all": "npm run build:customer && npm run build:admin && npm run build:kitchen && npm run build:rider",
    "install:all": "cd Frontend/customer && npm install && cd ../admin && npm install && cd ../kitchen && npm install && cd ../rider && npm install && cd ../../Server && npm install",
    "start": "cd Server && node index.js",
    "dev:server": "cd Server && npm run dev"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.3.0"
  }
}
